<%= link_to gravatar_for(current_user, size: 50), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to "view my profile", current_user %></span>
<stan><%= link_to request.base_url + "/" + current_user.webPage,request.base_url + "/" + current_user.webPage %></stan>
<span><%= pluralize(current_user.proyects.count, "proyect") %></span>